# 录入方法编辑器 — 逗号 / 分号 使用规则

## 核心约定

| 分隔符 | 含义 | 中英文 |
|--------|------|--------|
| **逗号** `,` `，` | **OR**（任一命中） | 都识别 |
| **分号** `;` `；` | **AND**（均需命中） | 都识别 |

> 一句话：**要"或"用逗号，要"且"用分号。** 中文标点（`，；`）和英文（`,;`）效果相同。

---

## 各字段用法

### 1. 固定参数「触发条件」

**多值 OR**（同一字段，任一命中）— 逗号：
```
检测项目=可溶性铅（Pb），可溶性镉（Cd），可溶性汞（Hg）
```
→ 项目名含铅/镉/汞任一即命中

**多条件 AND**（跨字段，均需命中）— 分号：
```
检测项目=可溶性六价铬（CrVI）；标准值=≤0.005
```
→ 项目名含 CrVI **且** 标准值含 ≤0.005

**组合**（OR 内层、AND 外层）：
```
检测项目=可溶性铅（Pb），可溶性镉（Cd）；标准值=≤0.05
```
→ (铅 OR 镉) AND 标准值≤0.05

可用字段：`检测项目` / `检测方法` / `标准值` / `默认`（无条件）

### 2. 固定参数「参数设置」（双击编辑）

多个参数用**分号**分隔：
```
称样量m(g)=0.5000; 稀释因子F=2.5; 样品浓度C(μg/L)=<0.020
```

### 3. 试样描述（desc）

用于：方法切换规则、条件设备规则、条件实验过程规则。

**逗号 OR**：描述含任一关键字
```
纸,木头   →   含"纸"或"木头"
```

**分号 AND**：描述需同时含全部
```
纸;涂层   →   同时含"纸"和"涂层"
```

**组合**：
```
纸,布;涂层   →   (纸 OR 布) AND 涂层
```

### 4. 标液「配制序号」（多标液）

**逗号 OR**（多个标液各自校验/提交）：
```
D-9376，D-9377，D-9387
```

### 5. 序列表「设备编号」（多设备）

**分号**分隔多台检测设备：
```
CK-SB017-CG;CK-SB063-CG
```

---

## 注意事项

- **分号在试样描述里是 AND**：旧配置若用分号当 OR，需改成逗号。
- 固定参数值避免笔误（如 `<<0.020` 双 `<` 应为 `<0.020`），否则服务端无法识别。
- 触发条件勿重复字段前缀（如 `检测项目=检测项目=...`）。

## 速记表

| 场景 | 分隔符 | 含义 |
|------|--------|------|
| 同类多值（任一） | 逗号 `,` | OR |
| 跨条件 / 多设备（都要） | 分号 `;` | AND |
